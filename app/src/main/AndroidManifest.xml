<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="Calendrier"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Calendrier">

        <!-- Launcher: fake calendar -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activation screen (first launch only) -->
        <activity
            android:name=".ActivationActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Secret browser - hidden from recents -->
        <activity
            android:name=".BrowserActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:noHistory="false" />

        <!-- Background tap detector service -->
        <service
            android:name=".TapDetectorService"
            android:exported="false" />

    </application>

</manifest>
